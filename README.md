# Data
Data
print"nikoo"
## ساختار پروژه

- `src/net.py` — اسکریپت تست شبکه
- `doc/plan.md` — برنامه‌ریزی توسعه
- `cfg/set.cfg` — تنظیمات اولیه

## قواعد پیام کامیت

- افزودن: `add read`, `add net`, `add plan`, `add set`
- اصلاح: `fix read`, `fix net`, `fix plan`, `fix set`
- حذف: `del pic`, `del log`
- تغییر ساختار: `mod src`, `mod doc`, `mod cfg`
